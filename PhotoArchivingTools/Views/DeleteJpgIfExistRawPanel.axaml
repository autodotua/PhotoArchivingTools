<UserControl
    x:Class="PhotoArchivingTools.Views.DeleteJpgIfExistRawPanel"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:PhotoArchivingTools.ViewModels"
    d:DesignHeight="450"
    d:DesignWidth="800"
    x:DataType="vm:DeleteJpgIfExistRawViewModel"
    mc:Ignorable="d">
    <Grid
        Margin="8"
        RowDefinitions="Auto,8,Auto,8,Auto,8,*,8,Auto">
        <TextBlock
            Text="删除目录中存在同名RAW文件的JPG文件"
            TextWrapping="Wrap" />
        <StackPanel
            Grid.Row="2"
            Orientation="Horizontal"
            Spacing="8">
            <TextBlock
                VerticalAlignment="Center"
                Text="RAW文件后缀：" />
            <TextBox Text="{Binding RawExtension}" />

        </StackPanel>
        <Button
            Grid.Row="4"
            Command="{Binding InitializeCommand}"
            Content="初始化" />

        <ListBox
            x:Name="lstDirs"
            Grid.Row="6"
            DisplayMemberBinding="{Binding Name}"
            ItemsSource="{Binding DeletingJpgFiles}" />

        <Button
            Grid.Row="8"
            Command="{Binding ExecuteCommand}"
            Content="执行" />
    </Grid>
</UserControl>
